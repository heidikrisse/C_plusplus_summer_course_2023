/* main.cpp:
 *
 * A program to manage a set of temperature sensor data. The main-loop
 * calls the user_interface function which manages the interaction with the
 * user
 */

#include "user_interface.h"

int main()
{
    user_interface();

    return 0;
}
