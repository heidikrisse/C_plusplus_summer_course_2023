/* main.cpp
 *
 * A program to read a file and print out the file statistics: the number of lines, words, chars in a file and the total size of the text files in a directory.
 */
#include "command_handler.h"

int main(int argc, char *argv[])
{
    handle_arguments(argc, argv);

    return 0;
}
